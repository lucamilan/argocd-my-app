apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: my-app

commonAnnotations:
  argocd.argoproj.io/sync-wave: "2"

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - final-check-job.yaml
